import { CTMark } from '../salon';

export function ClawTalkMark(): JSX.Element {
  return (
    <span className="clawtalk-sidebar-brand-mark" aria-hidden="true">
      <CTMark size={22} />
    </span>
  );
}
