# Talk Salon 3-Round Pilot Report

Status: merged via PR #586; follow-up polish merged in PRs #587–#591 (address chips, user-message identity, Talk sidebar, profile menu, Settings pages). The remaining-gaps list below feeds the final Talk visual polish pass.

## Scope

This lane only moves the Talk conversation screen toward the Claude Salon mock. It does not delete backend conversation APIs or change send/cancel/guardrail/run orchestration behavior.

Reference inputs:

- [talk-salon-gap-analysis.md](./talk-salon-gap-analysis.md)
- `docs/prototypes/prototype/shell.jsx`
- User-provided Claude mock screenshot from 2026-06-08.

## Screenshot Evidence

Generated by `npm --prefix webapp run test:e2e -- talk-fidelity.spec.ts`:

- `webapp/playwright/__screens__/talk-populated-desktop-1280.png`
- `webapp/playwright/__screens__/talk-populated-mobile-390.png`
- `webapp/playwright/__screens__/talk-empty-desktop-1280.png`
- `webapp/playwright/__screens__/talk-empty-mobile-390.png`
- `webapp/playwright/__screens__/talk-active-desktop-1280.png`
- `webapp/playwright/__screens__/talk-active-mobile-390.png`

## Round Score History

| Round | Evaluator Focus | Score | Result |
| --- | --- | ---: | --- |
| 0 | Baseline harness against old Talk view | 2/10 | Added mocked desktop/mobile fidelity spec for populated, empty, and active states. |
| 1 | Timeline/message delta | 6.5/10 | Added Salon timeline, avatars, bylines, round dividers, sanitized markdown rendering, model/status badges, and existing token counts. |
| 2 | Composer/top controls | 7.5/10 | Restyled composer with `ADDRESS TO`, agent chips, mode/round chips, and `Send to room`; compacted top pills and removed the old agent-health strip from visual hierarchy. |
| 3 | Layout/mobile cleanup | 8.2/10 | Removed visible conversation rail from Talk view, fixed prompt/run grouping, hid completed ordered-summary chrome unless action is needed, reduced mobile header chrome, and pinned the composer into the mobile workflow viewport. |

## Fixed Gap Checklist

- [x] Render markdown instead of showing literal `**bold**`.
- [x] Use Salon cream palette, serif message body, and mono metadata.
- [x] Add colored avatars for user and agents.
- [x] Add round dividers.
- [x] Add assistant byline metadata: derived handle, model, status, ordered step, timestamp, and token counts when available.
- [x] Surface existing `runs.tokens_in/out` through the Talk run DTO.
- [x] Derive `@handle` from nickname/role without schema changes.
- [x] Visually remove the conversation rail from the Talk conversation view.
- [x] Restyle composer with address-to chips, mode/round chips, and `Send to room`.
- [x] Add Playwright fidelity coverage for 1280 and 390 widths across populated, empty, and active states.

## Remaining Gaps

- Top pill semantics are still mostly existing routes: `Documents`, `Jobs`, and `Run History` remain visible instead of a final `Document` + overflow `...` mapping.
- The mock's breadcrumb row (`Research & longreads > ORDERED MODE > 2 AGENTS`) is not implemented.
- Token counts show only for persisted runs with stored values; streaming usage remains hidden until the existing stream path has clean data.
- Mobile shows only the active Talk pill in the header; fuller mobile section navigation still needs the final top-pill/overflow product mapping.
- Exact pixel spacing, avatar colors, and type scale are close enough for the pilot but still need a final visual polish pass.

## Joseph Verification

Optional visual review only:

- Open the PR screenshots and compare Talk populated desktop/mobile against the Claude mock.
- No product decision is required for this pilot unless changing the top-pill route mapping now.
