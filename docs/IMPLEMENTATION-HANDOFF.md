# ClawTalk Implementation Handoff

> **Status:** active handoff memory · **Last updated:** 2026-06-05
> Orientation: [REFACTOR-OVERVIEW.md](./REFACTOR-OVERVIEW.md) · readiness: [IMPLEMENTATION-READINESS.md](./IMPLEMENTATION-READINESS.md) · roadmap: [roadmap.md](./roadmap.md)

## Where We Are

Worktree: `/Users/josephkim/.codex/worktrees/381b/clawtalk`

Branch: `codex/clawtalk-greenfield-cutover` (35 commits ahead of `main`).

**The backend/runtime cutover is committed.** The legacy context/runtime execution surface is retired, disabled models fail closed at chat enqueue, and the webapp shell now drives the greenfield per-request workspace model. We are in **Phase 5 (frontend rewrite)**: the workspace switcher has landed, the **fresh-project cutover runbook** is written (`docs/CUTOVER-RUNBOOK.md`), and `TalkDetailPage.tsx` decomposition is underway — **Slice 1** deleted the retired State card (9538→9367 LOC) and **Slice 2** (`cfc3274`) extracted `TalkContextPanel` (Goal + Rules; page −428 LOC). **The shell + Slice 1 were visually verified in the local dev stack on 2026-06-03** (workspace name top-left; a Talk's Context tab renders Goal / Rules / Saved Sources / Bound Drive Resources with no State card). **Slice 2 is committed + review-gated (codex clean, 315 webapp tests green) but not yet Joseph-visual-verified** — a behavioral UI refactor; a local-stack pass on Goal save / rule add-edit-toggle-reorder-delete is the recommended next check. **Slices 3 (`e40b257`, orphaned `/state` compat surface removed) + 4 (`a9f8e45`, Jobs tab → `TalkJobsPanel`) have since landed** — `TalkDetailPage.tsx` is now **7830 LOC** (down from 8931). Slice 4 is committed + codex-clean (2 adversarial rounds) + 316 webapp tests green, but **not yet Joseph-visual-verified**.

**2026-06-04 — the autonomous Track A queue (`docs/PHASE5-AUTONOMOUS-PLAN.md`) is now executing under a THREE-pass review gate (see Review Gate below).** **A1 (`f427548`, PR #519)** added the composer @-mention characterization net (tests-only). **A2 (`f8f38b7` PR #520 + `62c9dbe` PR #521)** extracted `TalkAgentsPanel` + `TalkRunsPanel` (`connectors` was already `TalkConnectorsPanel`). **A3 (`c1407ab` PR #523)** extracted the composer → `TalkComposer` (the big 30+-dep seam; 35 props). **`TalkDetailPage.tsx` is now 7010 LOC (was 7830 at session start, −820).** All merged + deployed. **A3 is a §6 MILESTONE — composer needs a Joseph browser spot-check** (click-list posted; non-blocking, work continues). Next is **A4 (thread + streaming → `TalkThread` + `useTalkRunStream`)**, then A5 documents (data-contract change), A6 de-facade. **A4 scope (mapped):** the timeline render is `<div className="talk-thread-detail">` at **~L6309** through the `LiveResponsePanel` + new-message indicator (~L6730, where `<TalkComposer/>` now sits); it has **40+ deps** (timelineRef/endRef/`messageElementRefs`, `activeThread`, ordered-round progress `activeOrderedProgress`/`latestOrderedRound`, `retryRunState`, `pageMessages`/timeline entries, live responses, load-earlier + unread-indicator, run-history + edit + browser-block handlers). The streaming state machine is `useReducer(talkReducer, …)` at **~L1747** (reducer fn + ~15 action types) wired to `openTalkStream` callbacks. **Recommended split: A4a = `TalkThread` render (verbatim-JSX presentational, page-owned state threaded — proven A2/A3 pattern; existing streaming tests [T1 regression, concurrent live responses, MESSAGE_APPENDED] are the safety net), then A4b = `useTalkRunStream` hook (reducer + dispatch + stream callbacks → custom hook).** A4 is streaming-critical (reply-vanish bugs live here) and a §6 milestone — verify the composer (A3) in the browser first, since A4 stacks on the same Talk tab. **A4a SHIPPED (`f2e0d8c`, PR #532, deployed) as `TalkThreadView` (render-only; `TalkDetailPage.tsx` 7018→6666 LOC). A4b-1 SHIPPED (`2f7d727`, PR #535, deployed): reducer + types + helpers → `webapp/src/lib/talkRunReducer.ts` (behavior-preserving; page 6673→5719 LOC). A4b-2 SHIPPED (`25bdc77`, PR #537, merged on green CI + deployed): the `openTalkStream` effect → `webapp/src/hooks/useTalkRunStream.ts` (behavior-preserving; page 5719→5347 LOC). `useReducer` deliberately stayed page-owned and the hook returns void — a `resyncTalkState`→`dispatch` TDZ cycle blocks the planned hook-owns-reducer / returns-`{ state, dispatch }` shape, so the hook encapsulates only the effect. **A4 (thread + streaming) is COMPLETE. Next is A5 — Documents UI (a data-contract change → DESIGN PASS FIRST, fresh session).**

There is **no staged/uncommitted slice**. Each slice below was committed only after passing the review gate.

**Review-gate mechanics in this worktree (important).** The gstack `/review` and `/karpathy-audit` skills `git diff` the **cwd**, which is the *main* checkout, not this worktree — so invoking them via the Skill tool reviews the wrong tree. The working substitute for the cross-model adversarial pass is **`codex exec review --uncommitted`** run from inside the worktree (it reviewed Slice 1's staged diff and returned a clean verdict). Apply Karpathy's four principles to the diff by inspection. `codex` is at `~/.bun/bin/codex` (v0.125.0).

## Review Gate (per slice)

**THREE passes (Joseph, 2026-06-04 — raised from two)**, run against the uncommitted slice (`git diff HEAD`) so each sees exactly the slice, not the 46-ahead branch:

1. **gstack `/review`** (Claude adversarial + scope-drift check) — the Skill-tool invocation runs its bash in the cwd = *main* checkout (which sits on `main` → "nothing to review"), so it can't see this worktree. Run it manually: dispatch a fresh-context `general-purpose` Agent told to `cd` the worktree and adversarially review `git diff HEAD` + confirm scope.
2. **`/karpathy-audit diff`** — Karpathy's four principles on the slice diff, by inspection.
3. **Codex adversarial** — `~/.bun/bin/codex review --uncommitted` from inside the worktree (reviews staged+unstaged+untracked). This single run satisfies both Joseph's explicit standalone-Codex ask AND the Codex half that `/review` would otherwise bundle — don't spend Codex twice. Honor a `block`: adjudicate (real? in-scope? introduced here?), fix or justify.

Commit only when all three are clean or findings are fixed/justified. See memory `[[two-gate-review-process]]` (name historical).

## Slices committed this session

| Commit    | Slice                                                                                                                                                                                                                                |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `951ab34` | **refactor: retire legacy context runtime.** Fresh-baseline `talk_agent_snapshots.provider_id` / `messages.metadata_json` / private `message_provider_replay`; replay blobs stripped from member-readable metadata and persisted only in `message_provider_replay` with source-agent + frozen-snapshot scope and a shared byte budget; runtime/API identity reads frozen snapshot `provider_id`/`model_id`; active source refs are raw `context_sources.id::text`; `CleanTalkExecutor` fails closed with `LEGACY_EXECUTOR_RETIRED` (`new-executor.ts` keeps only the shared tool/image helpers); job snapshot creation skips non-target roster agents whose provider is unavailable while still blocking if the target provider is unavailable. |
| `6c40fb7` | **fix: gate disabled models at chat enqueue (fail closed like jobs).** Chat roster resolution filters `llm_provider_models.enabled = true`, so a disabled/retired model yields a null `provider_id` and the existing guard returns `agent_model_not_found` — matching the job path's `loadRoster`. Gated at snapshot creation (not the executor) to preserve frozen identity. |
| `5bb6712` | **feat(webapp): workspace switcher + per-request workspace scoping.** Sidebar switcher backed by the greenfield per-request model: no persisted active workspace; the client tracks it in a localStorage marker and sends `x-workspace-id` on every workspace-scoped request (without overriding an explicit `?workspaceId=`). Switching validates via POST, records the marker, and does a clean reload that discards in-flight old-workspace requests. Persist buster is workspace-scoped; `getSessionMe` self-heals a stale marker (`workspace_forbidden` → clear + retry); sign-out clears it. |
| `464e1d7` | **fix(dev): point webapp proxy + wrangler dev at :8788.** `vite.config.ts` proxy target stale `3210`→`8788` + pinned `wrangler dev` to `:8788` via `[dev] port` (bare wrangler defaults to 8787). Dev-only. |
| `decf546` | **docs: fresh-project cutover runbook** (`docs/CUTOVER-RUNBOOK.md`). Ordered, copy-pasteable; from-zero `db reset` re-validated locally. Surfaces the silent W7-gate no-op + the password-less `clawtalk_event_hub` role / `DB_EVENT_HUB_URL` gotcha. |
| `563e17f` | **refactor(webapp): delete retired Context State card** (first `TalkDetailPage.tsx` decomposition slice; 9538→9367 LOC). Removed `TalkStateCard` + `getTalkState`/`deleteTalkStateEntry` + `TalkStateEntry` + state/effect/render + dead `.talk-state-*` CSS + 7 tests + mock wiring. `resyncTalkState` (unrelated) left intact. `codex exec review` clean. |
| `cfc3274` | **refactor(webapp): extract `TalkContextPanel` (Context Goal + Rules)** (second `TalkDetailPage.tsx` decomposition slice; page −428 LOC). New `webapp/src/components/TalkContextPanel.tsx` (sibling of `SavedSourcesPanel`/`TalkToolsPanel`) houses `sortRulesByOrder`/`reorderRules`/`RuleRow`, the 6 goal/rule handlers, `ruleSensors`, and the Goal+Rules render. **Design pivoted away from the turnkey plan below: the panel is _presentational_ — all state stays page-owned (`goal`, `rules`, the shared load/mutation `contextStatus`, and the goal/rule drafts, like `jobDraft`) and is threaded as props.** A tab-mounted panel _unmounts on tab switch_, so panel-local state loses in-flight saves / the `'saving'` lockout / unsaved drafts (codex caught all three as remount races across 7 adversarial passes). Drafts are **sparse overrides** (`goalDraft: string\|null`, `ruleDrafts` only for edited rows) rendered from the live props and cleared on save via **id-keyed** deletes — `buildRuleDraftMap` dropped. Behavior-preserving except 401s now route through `onUnauthorized` (house pattern). `codex exec review --uncommitted` clean; Karpathy pass; typecheck + 315 tests + build green. |
| `a9f8e45` | **refactor(webapp): extract `TalkJobsPanel` (Jobs tab)** (fourth `TalkDetailPage.tsx` decomposition slice; 8931→7830 LOC, −1101). New `webapp/src/components/TalkJobsPanel.tsx` (+1250) houses the 8 job-only helpers/consts, the derived memos, the 4 fetch fns, the 9 handlers, the jobs-load effect, and the full Jobs render. **The panel is _presentational_ (cf. `TalkContextPanel`) — NOT self-fetching, contrary to the turnkey plan below.** `codex exec review --uncommitted` caught two remount races that the self-fetch design baked in and forced the pivot: (1) a re-fetched job list would resurface a clean-but-stale draft as a spurious "unsaved" diff savable over newer server state; (2) a save/delete/pause/run **in flight when the panel unmounts** updates the orphaned panel-local list while the page-owned selection/status advance → created job vanishes / deleted job lingers until refresh. **Resolution: every Jobs piece an async mutation writes — list, selection, draft, runs, mutation status — is page-owned and threaded in (21 props); load-once via page-owned `talkJobsLoaded` so nothing re-fetches on remount.** Only the read-only **tool scope** (`jobToolsState`/`jobToolsLoaded`/`jobToolsStatus`, never mutated) is self-fetched per mount (cf. `TalkToolsPanel`; minor "Loading job tools…" flash on tab re-entry). Page couplings behind callbacks: `onUnauthorized` + `onJobRunSettled` (wraps `activeThreadIdRef` + `resyncTalkState`). `agentOptions` pre-built on the page (panel needs neither `TalkAgent` nor `buildAgentLabel`). Behavior-preserving. codex clean (2 rounds); Karpathy pass; typecheck + 316 tests + build green. |
| `f427548` | **test(webapp): characterize composer @-mention typeahead (A1).** First slice of the *autonomous* Track A queue (`docs/PHASE5-AUTONOMOUS-PLAN.md`). +13 characterization tests pinning the composer @-mention surface (word-boundary trigger, filter, ArrowDown/Enter+Tab+click insertion, click-outside/Escape/Backspace dismissal, ready-only eligibility + @doc presence/absence, mid-draft splice) ahead of the composer extraction (A3); **tests-only, no source change**. webapp **326 tests** + typecheck + build green. Review (new 3-pass gate): codex `review --uncommitted` → *land* (caught + fixed one single-option click false-green), Claude adversarial subagent → *approve* (scope PASS), Karpathy clean. Merged **PR #519**. |
| `f8f38b7` | **refactor(webapp): extract `TalkAgentsPanel` (A2a).** Inline Agents tab → presentational `webapp/src/components/TalkAgentsPanel.tsx` (JSX verbatim); page keeps all agent state + 7 handlers, threads 18 props. Shared `TALK_AGENT_ROLE_OPTIONS`/`formatTalkRole`/`AgentCreationDraft` → new `webapp/src/lib/talkAgents.ts` (no cycle). +Remove-agent characterization test (primary promotion + disable-at-one; mutation-verified). 7830→7553 LOC. 327 tests + typecheck + build green; codex (no actionable regressions) + Claude adversarial (approve; byte-identical JSX) + Karpathy clean. Merged **PR #520**. |
| `c1407ab` | **refactor(webapp): extract `TalkComposer` (A3 — the big seam).** Composer `<form>` (target chips, char count, guardrail banner, `@`-mention textarea+picker, attachment chips, attach/cancel/send) → presentational `webapp/src/components/TalkComposer.tsx` (JSX verbatim, **35 props**). All mutation-written state + the `textareaRef`/`fileInputRef` stay page-owned and threaded in (so the page's `@`-mention cursor math + autosize effects still target the composer textarea). `buildAgentLabel` + `TalkAgentExecutionGuardrail` → lib `talkAgents.ts`; 3 icons + `buildAgentChipLabel` → into the composer. No cycle. 7323→7010 LOC. 327 tests + typecheck + build green; codex (no actionable correctness issues) + Claude adversarial (APPROVE; char-identical form, all 35 props verified no-swap) + Karpathy clean. Merged **PR #523**. **⚠️ MILESTONE — needs Joseph browser spot-check (composer).** |
| `62c9dbe` | **refactor(webapp): extract `TalkRunsPanel` (A2b).** Inline Run History tab → presentational `webapp/src/components/TalkRunsPanel.tsx` (JSX verbatim); 3 runs-only helpers (`formatPersonaRoleLabel`/`renderRunContextSnapshot`/`summarizeMessageForRun`) + `RunContextPanelState` type moved in; page keeps `runContextPanels` + handlers, threads `runHistory`/`messageLookup`. Dropped now-unused `ExecutionDecisionSummary` page import. Caught+fixed a latent copy bug (switch-based `formatPersonaRoleLabel` vs the real title-caser) before review. 7553→7323 LOC. 327 tests + typecheck + build green; codex (land as-is) + Claude adversarial (approve; char-identical helpers) + Karpathy clean. Merged **PR #521**. |
| `f2e0d8c` | **refactor(webapp): extract `TalkThreadView` (A4a — thread timeline render).** First half of A4. The `talk-thread-scroll` message-timeline render (`talk-thread-detail-header`, ordered progress/summary, the message/browser-run/live-response timeline map, unread indicator) → presentational `webapp/src/components/TalkThreadView.tsx` (JSX verbatim, **29 props**). Named `TalkThreadView` **not** `TalkThread` (collides with the `TalkThread` data type from `../lib/api`). Zero hooks — purely presentational, all state page-owned + threaded. 3 page→prop substitutions at the moved sites (`snapshotQuery.isPending`→`isSnapshotPending`, `state.runsById`→`runsById`, `state.hasUnreadBelow`→`hasUnreadBelow`). Moved `isRenderableImageAttachment`+`buildTalkAttachmentContentUrl` into the component; moved `formatThreadLabel` → `lib/threadTitles.ts` (page thread rail still uses it via import). Exported `OrderedRoundSummary`+`TalkTimelineEntry` from the page for the prop types (`RunView`/`LiveResponseView` already exported; matches `LiveResponsePanel`). Removed 4 now-dead page imports the extraction orphaned (Claude-review finding). 7018→6666 LOC. 3-gate clean (codex no-block; Claude APPROVE — token-equal JSX; Karpathy). typecheck + 329 tests + build green; `TalkDetailPage.test` 79/1-skip unchanged. Merged **PR #532**. **⚠️ §6 MILESTONE + streaming-critical — Joseph browser spot-check of thread/streaming recommended.** |
| `2f7d727` | **refactor(webapp): extract talk-run reducer → `lib/talkRunReducer.ts` (A4b-1).** First half of A4b. Behavior-preserving VERBATIM move of the reducer machinery out of the page into a new module: types (`RunView`, `LiveResponseView`, `OrderedRoundStepTone`/`Summary`, `OrderedRoundSummary`, `TalkTimelineEntry`, `DetailState`, `DetailAction`), `detailReducer`, `createInitialDetailState`, `getOrderedStepTone` (exported); `toRunView`/`mapRunsById`/`deriveLiveResponsesFromRuns`/`pruneEventRunCache`/`isNonTerminalRunStatus`/`clearFailedLiveResponsesForThread`/`shouldShowInlineFailure`/`withRun` + `MAX_EVENT_RUN_CACHE` (module-internal). Page keeps `useReducer` + all 48 dispatch sites unchanged; imports back `detailReducer`/`createInitialDetailState`/`getOrderedStepTone` + 3 view types (`RunView`/`OrderedRoundSummary`/`TalkTimelineEntry`). Repointed `TalkThreadView`/`LiveResponsePanel`(+test) view-type imports `../pages/TalkDetailPage` → `../lib/talkRunReducer`. Removed 2 now-dead page imports (`stripInternalAssistantText`, `TalkStreamState`). **Plan-anchor corrections (verified against code): there is no `chunk` helper, and `isNonTerminalRunStatus`/`pruneEventRunCache` are reducer-INTERNAL (no external caller) → kept module-private, not exported.** 6673→5719 LOC. Extraction via boundary-asserted python slice; let tsc validate the closure. typecheck + build + **354/1-skip** webapp tests green (`TalkDetailPage.test` 79/1-skip — streaming characterization net intact). 3-gate clean (codex no actionable bugs; Claude APPROVE byte-faithful + complete-closure + tight-scope; Karpathy pass). Merged **PR #535** (admin), deployed ✓. |
| `25bdc77` | **refactor(webapp): extract `openTalkStream` effect → `hooks/useTalkRunStream.ts` (A4b-2).** Second half of A4b — the risky streaming-rewire. Behavior-preserving VERBATIM relocation of the ~375-line `openTalkStream` subscription effect (body + 12-entry dep array byte-identical) out of the page into a new hook; `MISSING_PERSISTED_MESSAGE_REFETCH_MS` moved in with it. **`useReducer` deliberately STAYED page-owned (NOT moved into the hook as the plan sketched, and the hook returns void — NOT `{ state, dispatch }`):** `resyncTalkState` — a closure dep of this effect — itself calls `dispatch` (L1827), so a hook that both produced `dispatch` and consumed `resyncTalkState` is a temporal-dead-zone cycle at the call site; moving `resyncTalkState` in explodes scope (used by `refreshBrowserRuns`/`handleJobRunSettled` + the thread-state web); dropping callbacks from the dep array to break the cycle would alter re-subscription timing (the reply-vanish surface). So the page keeps `useReducer` + all 48 dispatch sites and threads `dispatch` + the effect's closure deps (12 dep-array entries + 10 stable refs + 5 stable setters) in as params; refs/setters stay OUT of the dep array exactly as the original (stable by construction). *(Design fork surfaced to Joseph via AskUserQuestion; he chose the verbatim relocation.)* Removed now-dead page imports (`openTalkStream`, 19 stream event types, `applyMessageAppendedDelta`). 5719→5347 LOC. Extraction via boundary-asserted python slice; tsc + tests validated the closure. typecheck + build + **354/1-skip** webapp tests green (`TalkDetailPage.test` 79/1-skip — streaming characterization net intact, IDENTICAL to the pre-slice baseline). 3-gate clean (codex `review --commit HEAD` no blocks, ran the suite itself; Claude adversarial APPROVE — diff'd both effect bodies = 375 lines exit 0, 28-param three-way match so swaps structurally impossible; Karpathy 4-principles by inspection pass). Merged **PR #537** on GREEN CI (not admin — backend CI is green since #530), deployed ✓. |

## Next steps (in order)

### A4b — thread streaming hook. **A4b-1 SHIPPED `2f7d727` (PR #535, merged + deployed)** — reducer + types + helpers → `webapp/src/lib/talkRunReducer.ts` (see slice-table row; behavior-preserving, 3-gate clean, page 6673→5719 LOC). **A4b-2 SHIPPED `25bdc77` (PR #537, merged on green CI + deployed)** — `openTalkStream` effect → `webapp/src/hooks/useTalkRunStream.ts` (see slice-table row; behavior-preserving, 3-gate clean, page 5719→5347 LOC). **A4 (thread + streaming) is COMPLETE.** §6 streaming milestone — browser spot-check posted on PR #537 (send a prompt → reply streams, scroll-follows to bottom, stays after settle).

**Shape map (re-verify line numbers — they drifted ~−954 after A4b-1 shrank the page to 5719 LOC):**

- `detailReducer` + `createInitialDetailState` now live in **`webapp/src/lib/talkRunReducer.ts`** (exported, A4b-1). The page now only `useReducer(detailReducer, undefined, createInitialDetailState)` at **~L771** (was L1747).
- **48 `dispatch({…})` sites**: **19 inside the `openTalkStream` effect** (`const stream = openTalkStream({` now at **~L2235**, was ~L3189; these move into the hook), **29 in page handlers** (send/cancel/thread-select/unread/reset/snapshot-hydrate/message-landed; these STAY and use the hook's returned `dispatch`).
- The `openTalkStream` effect (~380 lines) closes over a lot: `queryClient`/`wsCacheRouterRef`, `talkId`/`userId`, `handleUnauthorized`, `activeThreadIdRef` + scroll refs (`autoStickToBottomRef`), refetch/scroll callbacks. This is the closure-rewiring risk.
- ✅ Reducer helper deps already moved into the module by A4b-1 (`toRunView`/`mapRunsById`/`deriveLiveResponsesFromRuns`/`pruneEventRunCache`/`isNonTerminalRunStatus`/`clearFailedLiveResponsesForThread`/`shouldShowInlineFailure`/`withRun` + `MAX_EVENT_RUN_CACHE`, all module-internal; `getOrderedStepTone` exported + imported back). A4b-2 just needs the reducer/types **from the module** — it does NOT re-move helpers.

**Recommended split (de-risk — do NOT big-bang the hook):**

- ✅ **A4b-1 — reducer + types → module (`webapp/src/lib/talkRunReducer.ts`) — DONE `2f7d727` (PR #535).** Behavior-preserving VERBATIM move: `detailReducer`, `createInitialDetailState`, `DetailState`/`DetailAction`, the view types (`RunView`, `LiveResponseView`, `TalkTimelineEntry`, `OrderedRoundSummary`, `OrderedRoundStepSummary`, `OrderedRoundStepTone`), the 5 reducer-only helpers + `toRunView` (transitive dep of `mapRunsById`), and `getOrderedStepTone` (exported). Page keeps `useReducer(...)` + all 48 dispatch sites unchanged; imports back `detailReducer`/`createInitialDetailState`/`getOrderedStepTone` + types `RunView`/`OrderedRoundSummary`/`TalkTimelineEntry`. Repointed the A4a type-imports in `TalkThreadView.tsx` + `LiveResponsePanel.tsx` (+ its test) FROM `../pages/TalkDetailPage` TO the new module; the two A4a page `export`s vanished automatically (the types themselves moved). Removed 2 now-dead page imports (`stripInternalAssistantText`, `TalkStreamState`). **Plan drift corrected against code: (1) there is NO `chunk` helper — phantom anchor; (2) `isNonTerminalRunStatus`/`pruneEventRunCache` are reducer-INTERNAL (no external caller via grep) → kept module-private, not exported (only `getOrderedStepTone` is page-shared).** Closure verified by tsc; the existing reducer/streaming characterization tests (T1 regression, concurrent live responses, MESSAGE_APPENDED) stayed green. 3-gate clean.
- ✅ **A4b-2 — `useTalkRunStream` hook — DONE `25bdc77` (PR #537).** Behavior-preserving VERBATIM relocation of the `openTalkStream` effect (body + dep array byte-identical) into `webapp/src/hooks/useTalkRunStream.ts`; `MISSING_PERSISTED_MESSAGE_REFETCH_MS` moved in with it. **Plan correction (verified against code): the hook does NOT wrap `useReducer` and does NOT return `{ state, dispatch }`.** `resyncTalkState` (an effect closure dep) calls `dispatch` (L1827), so a hook owning the reducer AND consuming `resyncTalkState` is a temporal-dead-zone cycle at the call site; `useReducer` + all 48 dispatch sites stay page-owned, and the hook takes `dispatch` + the effect's closure deps (12 dep-array entries + 10 stable refs + 5 stable setters) as params, returning void. Refs/setters omitted from the dep array (stable by construction) exactly as the original inline effect. Removed now-dead page imports (`openTalkStream`, 19 event types, `applyMessageAppendedDelta`). 3-gate clean; 354/1-skip webapp + 79/1-skip `TalkDetailPage.test` streaming net unchanged from the pre-slice baseline. **This was the last A4 slice — A4 (thread + streaming) is COMPLETE; next is A5 Documents UI (design pass first).**



1. ✅ **DONE (2026-06-03) — visual verification of the shell + Slice 1.** Confirmed live in the local dev stack: workspace name top-left; a Talk's Context tab renders Goal / Rules / Saved Sources / Bound Drive Resources with **no State card**. The local-login 403 fix (`CLAWTALK_ALLOWED_ORIGINS` incl. `http://localhost:5173`) is now in the worktree's gitignored `.dev.vars`, so future local-dev login works after `npm run db:start` + `dev:worker` + `dev:web`. **Slice 2 (below) is the next actionable step.** (Exercising the *switcher* would need a second workspace; there is intentionally **no create-workspace flow**.)
2. ✅ **DONE (`cfc3274`) — Slice 2: extract `TalkContextPanel` (Goal + Rules).** See the table row above. **Key lesson for future panel slices (the turnkey plan here was wrong on one axis):** a panel rendered inside `{currentTab === 'x' ? … : null}` **unmounts on every tab switch**, so it _cannot own_ state touched by async mutations or state that must persist across tab visits. The original plan ("Panel owns `goalDraft`/`ruleDrafts`/a mutation status", `key={talkId}` for "clean draft reset", TRAP-2 option **a**) all produced real remount regressions that `codex exec review` caught in sequence: stale-draft-revert, lost `'saving'` lockout → double-submit, lost unsaved drafts on tab switch, cross-talk draft clobber. **Resolution: keep state page-owned (like `jobDraft`) and make the panel presentational** — drafts + `contextStatus` live on the page; the panel gets them as props. TRAP 1 dissolved by the **sparse-override** model (render from the live prop, override only for actively-edited fields, cleared via id-keyed deletes); TRAP 2 resolved with option **b** (one page-owned status, threaded in — the original loading-first gate + tab-bottom success message preserved). Net: the panel ended ~15 props but behavior-identical. **`activeRuleCount` badge stays page-owned (reads `contextRules`); composer/`contextSources` untouched.**
3. ✅ **DONE (`a9f8e45`) — Slice 4: extract `TalkJobsPanel` (Jobs tab).** See the table row above. **Not yet Joseph-visual-verified** (behavioral UI refactor). **Local-stack click-list** (`db:start` + `dev:worker` :8788 + `dev:web` :5173, login via real Google OAuth): open a Talk → **Jobs** tab → (a) **New Job**, fill title/prompt/agent/schedule, **Save Job** → job appears in the list, "Job created."; (b) select it, edit the prompt, **switch to another tab and back** → your edit is still there (page-owned draft survives remount); (c) toggle a weekday + a tool-scope checkbox, **Save** → persists; (d) **Run Now** → status cycles queued→…→completed and Recent runs updates; (e) **Pause**/**Resume** flips status; (f) **Delete** → removed, next job auto-selected (or "Select or create a job" when none). Watch for: a job created/deleted while you tab away then back must reflect correctly (the in-flight-mutation race codex flagged), and the brief "Loading job tools…" flash on tab re-entry is expected. **Lesson that sharpens the Slice 2 rule:** "self-fetch the data like `TalkToolsPanel`" is only safe for **pure read-only data that no page-surviving handler ever writes** (drive bindings, tool scope). Any state an async **mutation** writes — a list mutated by save/delete, runs updated by a poll — must be **page-owned**, because the mutation can resolve *after* the tab-switch unmount and would otherwise land on an orphaned panel-local copy while the page-owned selection/status advance (codex caught exactly this). When in doubt, default to fully presentational + page-owned (load-once via a page-owned `…Loaded` flag so nothing re-fetches on remount).
4. **Iterate other low-coupling seams.** The composer is last (30+ tangled deps). `SavedSourcesPanel` stays pinned to lifted `contextSources` until the composer/sources untangle (which is also when `['talk-context', talkId]` React-Query ownership can be introduced cleanly — the bundled `getTalkContext` returns sources, which the composer reads, so an RQ key can't be added without touching the composer). The other sizeable non-composer surface **was** the **`context-loader.ts` retirement** (below, ✅ now done). **Apply the Slice 2+4 lesson: extracted panels should be presentational with page-owned state; self-fetch only pure read-only data that no mutation writes.**

## Open follow-ups

- ✅ **DONE (`e40b257`) — orphaned `/state` compat surface removed.** Both routes + `getGreenfieldTalkStateRoute`/`deleteGreenfieldTalkStateEntryRoute` + the dead route-layer `validateStateKey`/consts/`TalkStateEntrySnapshot` + tests + the stale worker-app comment + the dead `/state`→context alias in `pathToTab`. Gated by backend typecheck + `greenfield-context.test.ts` (green, not part of the red legacy suite) + `codex exec review` clean. A separate **db-layer** `validateStateKey` copy in `context-accessors.ts` (used by db-layer state accessors) was intentionally left — it's part of the deeper db-accessor state cleanup, not this route slice. Also dropped the dead frontend `formatJsonForStateEditor` (Slice-1 orphan, `3f8de48`).
- ✅ **DONE (B2) — Settings follow-ups.** `SavedSourcesPanel` + `SourceMentionPicker` now display raw UUID source refs as stable human-readable `Source N` labels while preserving the raw ref/slug insertion contract. `selectProviderReplayMessageIds` has a focused budget-exceeded break test. Chat enqueue + job roster joins now require both `llm_provider_models.enabled` and `llm_providers.enabled`, with provider/model block copy aligned to the shared `model_disabled` reason.
- ✅ **DONE (B3) — Track-B webapp characterization backfill.** Added focused tests for the Settings extraction boundary without touching Track A files: `ProviderConfigPanel` scoped personal/workspace credential callbacks, admin-only workspace credentials, Anthropic OAuth scope threading, provider draft initialization; `AiAgentsSettingsPanel` registered-agent prop forwarding and Main Agent selector/save gating; `sourceDisplay` raw-UUID detection and label fallback behavior. Gated by root/webapp typecheck + targeted suites + full webapp test run + webapp build + codex/gstack, Karpathy, and Claude adversarial reviews.
- ✅ **DONE — `context-loader.ts` fully retired (whole-file deletion, not the partial trim this item predicted).** The plan-time audit found the **entire** 2801-line file dead-to-live-code: **all 15 exported functions and 9 of the 14 types had zero non-test callers** (the live runtime uses `greenfield-executor.ts`'s own `loadGreenfieldContextSources` / `loadGreenfieldContextSourceForRead`). The `agent-router.ts` / `google-drive-tools.ts` / `attachment-caps.ts` / `html-anchors.ts` references were **comments only**, not imports. The sole live surface was the `TalkRunContextSnapshot` type (+ its 4 sibling snapshot types), imported by `greenfield-detail.ts` + `talks.ts` — extracted to a new `talk-run-context-snapshot.ts`; the two route imports were repointed. Then deleted `context-loader.ts` + all three dead test files (`context-loader.test.ts`, `context-loader-readiness.test.ts`, and `effective-tools-integration.test.ts` — its only live touch, `getEffectiveToolsForAgent`, is covered by `agent-accessors.test.ts`). Net **−5886 LOC**. Gated by typecheck (tsconfig includes `src/**/*`, so it covers the test graph) + `greenfield-detail` / `agent-accessors` / `greenfield-executor` tests + `codex exec review` clean.
- ✅ **DONE (`a9f8e45`) — Jobs tab → `TalkJobsPanel`.** See the table row + Next-steps #3. Landed **presentational, not self-fetch** (the turnkey "self-fetch `talkJobs`/`selectedJobRuns`" plan was wrong: those are mutation-written, so an in-flight save/run resolving after unmount orphaned the panel-local copy — codex round 2). Two residual, pre-existing-flavored behaviors to know: (1) the read-only **tool scope re-fetches on every Jobs-tab re-entry** (`jobToolsLoaded` is panel-owned) → a brief "Loading job tools…" + momentarily-disabled tool checkboxes for ~1 RTT; harmless, self-heals, and the directive's intended "self-fetch tool scope". (2) **Jobs async-navigation races** (faithful copies of the original inline handlers, same class as the Context ones below): `handleSaveJob`/`handleRunJobNow`'s detached poll call page-owned setters without a talk/abort token, so a completion resolving after a talk switch applies to the wrong talk. Latent, single-user, sub-RTT; fix with a token only if real.
- **Pre-existing Context async-navigation races (surfaced by Slice 2 codex review, NOT introduced by it).** The goal/rule mutation handlers (in `TalkContextPanel`, faithful copies of the original inline handlers) call page-owned setters without guarding against a talk switch: a `handleSaveGoal` `setGoal(result.goal)`/`setStatus` or a `handleAddRule` append that resolves _after_ the user navigated to another talk applies to the **wrong** talk. Separately, the rule **drag handle is enabled during in-flight saves** (`RuleRow disabled={!canEdit}`, never gated on `'saving'` — also pre-existing), so a reorder during an in-flight rule-text PATCH can race on stale order. Both are latent in the original `TalkDetailPage`; fix with a talk/abort token across the context mutations **only if these become real** (single-user today, sub-RTT windows).

## Resume Prompt

```text
Continue the ClawTalk greenfield cutover from /Users/josephkim/.codex/worktrees/381b/clawtalk on branch
codex/clawtalk-greenfield-cutover — NOT the main checkout.

First read memory [[greenfield-cutover-active]] + [[two-gate-review-process]], then docs/REFACTOR-OVERVIEW.md,
docs/IMPLEMENTATION-HANDOFF.md, docs/roadmap.md. Verify doc claims against `git log` before trusting them.

The backend/runtime cutover is committed (legacy context runtime retired, disabled-model fail-closed enqueue,
workspace switcher). No staged slice. Per-slice gate = gstack /review (bundles Codex adversarial — honor block
verdicts) + /karpathy-audit diff. Commit only when both are clean or findings fixed/justified.

Current phase is the frontend rewrite (Phase 5). Before building more frontend, get Joseph's visual verification
of the shell. Then decompose webapp/src/pages/TalkDetailPage.tsx ONE isolated extraction at a time, each verified
visually. Do NOT big-bang it and do NOT build a create-workspace flow.

Backend slices: prettier pre-commit runs format:fix on src/ + scripts/ only — run `npm run format:fix && git add -u`
before commit. Backend tests: `CLAWTALK_ALLOW_UNSUPPORTED_NODE=1 npm run test -- <file>` (local node is v22).
```
