# TODOs

- [ ] If the OpenAI Codex OAuth poll API expands beyond the current `pending | authorized` frontend contract, update the SettingsPage poll loop to stop on terminal non-authorized statuses, such as expired or denied, and continue polling with backoff for recoverable responses such as slow_down. Today `webapp/src/lib/api.ts` only exposes `pending | authorized`, and backend provider poll errors are returned as API errors.
- [ ] Reconcile webapp upload caps with `src/shared/attachment-caps.ts`: `TalkDetailPage.tsx` still enforces 10 MB general / 5 MB image chat attachments while shared backend caps are 25 MB / 10 MB, and `SavedSourcesPanel.tsx` still has a separate 10 MB saved-source pre-check. Decide whether the stricter client UX is intentional; then either import the shared constants or keep the stricter limits with comments/tests.
- [ ] Decide whether browserless scheduled/manual runs need server-side PDF page rasterization via Cloudflare Browser Rendering. Client-side pdf.js rasterization covers user-opened/uploaded PDFs, but a scheduled run can consume a never-rendered PDF and fall back to extracted text only. Treat this as a narrow infra/product call because it adds Browser Rendering cost and operational surface.
